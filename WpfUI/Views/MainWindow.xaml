<Window x:Class="WpfUI.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfUI.Views" WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        Title="{Binding DisplayName}" Height="450" Width="800">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Grid.Row="0">
            <Label Content="{Binding MainTitle}" FontSize="15" FontWeight="Bold"/>
        </StackPanel>

        <Grid Grid.Row="1">
            <TabControl ItemsSource="{Binding Tabs}" SelectedItem="{Binding SelectedTab}">
                <TabControl.ItemContainerStyle>
                    <Style TargetType="TabItem">
                        <Setter Property="Header" Value="{Binding DisplayName}"/>
                        <Setter Property="Padding" Value="20, 10"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                    </Style>
                </TabControl.ItemContainerStyle>
            </TabControl>
        </Grid>
    </Grid>
</Window>
